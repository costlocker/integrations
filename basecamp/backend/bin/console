#!/usr/bin/env php
<?php

$app = require __DIR__ . '/../app/app.php';

$console = $app['console'];
$console->addCommands([
    new Costlocker\Integrations\Queue\ProcessSyncRequestsCommand(
        new Costlocker\Integrations\Queue\ProcessSyncRequest($app)
    ),
]);
$console->run();
