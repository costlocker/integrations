{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@uirouter/react": "^0.5.0",
    "clipboard": "^1.7.1",
    "immstruct": "^2.0.0",
    "immutable": "^3.8.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "yarn run build-css && react-scripts build",
    "build-css": "(cd ui-kit && yarn build)",
    "watch-css": "(cd ui-kit && yarn start)",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
